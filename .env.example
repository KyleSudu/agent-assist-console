# Fixture mode is deterministic, free, and does not require secrets.
# Supported values: fixture, anthropic, openai.
DRAFT_PROVIDER=fixture

# Remote adapters use explicit provider, model, and credential values.
MODEL_NAME=
MODEL_API_KEY=
